# MusicBot Environment Variables
# Copy this file to .env and configure your settings

# Discord Bot Token (Required)
DISCORD_TOKEN=your_discord_bot_token_here

# YouTube Data API Key (Optional - for 5-10x faster search performance)
# Get from: https://console.cloud.google.com/
# Enable: YouTube Data API v3
# Free tier: 10,000 requests/day
YOUTUBE_API_KEY=your_youtube_api_key_here

# Spotify Integration (Optional)
SPOTIFY_CLIENT_ID=your_spotify_client_id
SPOTIFY_CLIENT_SECRET=your_spotify_client_secret

# Search Performance Settings (Optional)
SEARCH_CACHE_TTL=3600           # Cache duration in seconds (default: 1 hour)
SEARCH_CACHE_MAX_SIZE=1000      # Maximum cached queries (default: 1000)

# Bot Behavior Settings (Optional)
IDLE_DISCONNECT_DELAY=120       # Auto-disconnect delay in seconds (default: 2 minutes)
LOG_LEVEL=INFO                  # Logging level: DEBUG, INFO, WARNING, ERROR

# Health Check Settings (Optional)
HEALTH_CHECK_INTERVAL=300       # Health check interval in seconds (default: 5 minutes)
MEMORY_CLEANUP_INTERVAL=600     # Memory cleanup interval in seconds (default: 10 minutes)
MAX_GUILD_DATA_AGE=86400        # Guild data max age in seconds (default: 24 hours)

# Web UI Settings (Optional)
WEB_UI_HOST=0.0.0.0            # Web UI host (default: 0.0.0.0)
WEB_UI_PORT=5000               # Web UI port (default: 5000)